import pandas as pd
from pathlib import Path


def load_dataset(path: str) -> pd.DataFrame:
    """
    Load a CSV dataset.

    Parameters
    ----------
    path : str
        Path to CSV file.

    Returns
    -------
    pd.DataFrame
    """
    pass